import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'scribbie',
  appName: 'scribbie-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
